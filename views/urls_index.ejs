<!DOCTYPE html>
<html lang="en">
<head>

  <title>TinyApp</title>


</head>
   <body>

     <header>
     <% include ./partials/_header %>
     </header>
       <h1 style="text-align: center;">urlDatabase</h1>
           <div class='urls'>
              <ul>
                 <% for(var i in urls) {%>
                    <li class="url"> <%= [i] + " ======> " + JSON.stringify(urls[i])%>
                       <form method="POST" action="/urls/<%= [i] %>/delete"><button class='delete'>Delete</button></form>
                          <a class="edit" href="/urls/<%= [i] %>">Edit</a>
                    </li>
                        <% }  %>

              </ul>
            </div>

    </body>
</html>